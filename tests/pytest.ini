[pytest]
# Ensure tavern is included in the pytest plugins
addopts = --tb=short --disable-warnings
plugins =
    tavern

tavern-global-cfg = config.yml
tavern-strict = json:off headers:off

log_cli = true
log_cli_level = INFO

filterwarnings =
    ignore::DeprecationWarning